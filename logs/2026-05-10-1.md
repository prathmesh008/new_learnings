## 2026-05-10 — React useCallback vs useMemo

Okay, so today my head kinda spun trying to get `useCallback` vs `useMemo` straight. Like, if `useMemo` can just return a function, `const handle = useMemo(() => () => { doStuff() }, [deps])`, what's even the point of `useCallback`? Is it just shorter to type or something? That specific nested arrow function in `useMemo` kinda messed me up. I feel like there's a deeper perf reason for `useCallback` that I'm just not grokking yet. Need to re-read that damn React docs page again tmrw.
